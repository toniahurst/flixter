<br />
<div class="booyah-box col-10 offset-1">
  <h1 class="text-center"><%= @course.title %></h1>
  <br />
  <div class="text-center">
    <%= link_to 'Preview as student...', new_instructor_course_section_path(@course), class: 'btn btn-primary btn-lg' %>
    <br class="clr">
  </div>
  <ul>
    <% @course.sections.each do |section| %>
      <li>
        <%= section.title %>
      </li>
    <% end %>
  </ul>

  
</div>